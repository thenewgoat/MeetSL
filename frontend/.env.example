# MeetSL Frontend Environment Variables
# Copy to .env.local and fill in values

# WebSocket URL for sign recognition stream (optional — defaults to auto-detect from window.location)
# NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/session

# Backend API URL for REST endpoints (optional — defaults to auto-detect from window.location)
# NEXT_PUBLIC_API_URL=http://localhost:8000
